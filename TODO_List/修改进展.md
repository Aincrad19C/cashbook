# 账本日历板块
### 删减功能
- 金额，不要0.01元这种精度的支持，全部改为整数元, **不需要**
- 删除**确认并继续**
- 流水归属、流水名称（不需要）

### 添加功能
- 支出/收入类型分析（修改：固定种类，饮食、交通、娱乐、其他等等，用户可以自定义）
- 必须填的内容需要显示指出
- 必填内容没有填完整的时候不要弹窗，之间在编辑框内提示即可
- 把"上下箭头"换成"加号""减号"以便于标识收入支出
- 在日期旁边添加一个日历组件便于查找
- 把收入支出的颜色调整 
- 把当日的日期"画圈显示"
- 增加"每月预算提示"在"当月分析"按钮旁边，以"本月预算剩余剩余XX%"的形式显示，右面加一个小问号或者">"，点击后显示预算详情，点击进去可以修改本月预算

# 数据分析板块
### 删减功能
- 每月流水统计： 
    - 建议解决方案，一次只能看半年, 可以通过左右箭头翻页查看其他月份的数据
    - 把全部归属和全部年份按钮删除
### 添加功能
- 在每月流水统计板块的下面增加切换按钮，切换年份(以半年为单位)
- 饼状图和柱状图只保留收支类型


# 流水管理板块
### 删减功能
- 完全不需要平账功能
- 不需要自助去重
- 把删除选中、类型修改按钮换成默认的"选择"按钮，在点击后，出现删除选中、类型修改、合并选项，同时所有流水左边出现选择框
- 去掉流水左边的选择框默认显示，只在点击选择按钮后显示
- 删除小票编辑

### 添加功能
- 数额大小排序/类型排序功能
- 添加筛选按钮在应用筛选之后的变色
- 多条记录合并为单条记录
- 在导入账单的时候增加识别类型功能(大致识别为几类，用户可以再修改,寻找api)

# 待收款板块
- 完全去掉此板块

# 预算管理板块
- 预算管理 ->
    - 删除刷新额度功能，自动刷新
    - 添加固定支出，(交互细节有问题，点两次保存出现问题)
- 预算管理：
  - 预算超支提醒，（本月过去了50%，预算花费了70%，具体的提醒形式可以再商量）

# 账本管理板块
- 完全去掉此板块
- 一个用户只有一个账本

# 类型管理板块
- 完全去掉此板块

# 文档站板块
- 完全去掉此板块

# Github板块
- 完全去掉此板块


# 整体修改
- 增加新手引导(气泡的形式，只在第一次注册登录时出现)
- 增加 帮助板块(里面放置每个板块的说明手册，同时里面有搜索功能)

